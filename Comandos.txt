init;Inicializa um novo repositório Git;git init|clone;Clona um repositório existente;git clone <url-do-repositório>|status;Mostra o status das alterações no repositório;git status|add;Adiciona arquivos ao índice (staging area);git add .|commit;Grava as alterações no repositório;git commit -m "<commit-message>"|branch;Lista, cria ou exclui branches;git branch|checkout;Muda para uma branch específica ou restaura arquivos;git checkout <branch>|merge;Une uma branch à branch atual;git merge <branch>|pull;Atualiza o repositório local com as alterações do repositório remoto;git pull|push;Envia as alterações do repositório local para o repositório remoto;git push|fetch;Baixa as alterações do repositório remoto sem integrá-las;git fetch|remote;Gerencia repositórios remotos;git remote|log;Exibe o histórico de commits;git log|diff;Mostra as diferenças entre commits, branches, etc.;git diff|reset;Reseta o índice e a árvore de trabalho para um estado anterior;git reset|rm;Remove arquivos do índice e da árvore de trabalho;git rm <arquivo>|stash;Armazena temporariamente as alterações não commitadas;git stash|tag;Cria, lista ou exclui tags;git tag|rebase;Reaplica commits em cima de outra base;git rebase|show;Mostra vários tipos de objetos do Git;git show|config;Define configurações do usuário para o repositório;git config|
